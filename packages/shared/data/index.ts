// !! IMPORTANT !! Don't modify the json schema directly. Use the "syncEnums" script to generate new property keys(syncs data from corresponding ./types file)

import food from './food.json';
import pets from './pets.json';
import adminIds from './adminIds.json';

export { food, pets, adminIds };
