{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "resolveJsonModule": true,
    "outDir": "dist"
  },
  "files": ["main.ts"],
  "include": ["data", "data/**/*.json", "composables", "types", "utils"],
  "types": [
    "bun-types" // add Bun global
  ]
}
