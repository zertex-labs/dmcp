{
  "name": "metadata",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sync": "bun run scripts/syncData.ts",
    "validate": "bun run scripts/validateData.ts"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
